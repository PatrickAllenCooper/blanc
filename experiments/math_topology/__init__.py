"""DeFAb-Math-Topology experiments package.

Mirrors experiments/ at the math level: benchmark generation, four-model
baselines, and the M2 Lakatos rediscovery / M3 at-scale loops.

Named ``math_topology`` (rather than ``math``) to avoid colliding with
Python's stdlib ``math`` module under the project's flat-import convention
(see e.g. ``experiments/finetuning/``).
"""
