% DeFAb pipeline diagram -- clean layout for NeurIPS column width
\begin{tikzpicture}[
    >=stealth,
    node distance=0.8cm and 1.0cm,
    box/.style={
        draw, rounded corners=2pt, minimum height=0.7cm,
        font=\small, inner sep=4pt, text centered, align=center
    },
    levelbox/.style={
        draw, rounded corners=2pt, minimum height=0.6cm,
        font=\small, inner sep=4pt, text centered, align=center,
        fill=black!4, minimum width=2.0cm
    },
    modbox/.style={
        draw, rounded corners=2pt, minimum height=0.6cm,
        font=\small, inner sep=4pt, text centered, align=center,
        fill=black!8, minimum width=1.5cm
    },
    arr/.style={->, thick},
    lbl/.style={font=\scriptsize, midway, above, text=black!60},
    stglbl/.style={font=\scriptsize\bfseries, text=black!50}
]

% --- Stage 1 ---
\node[box, text width=1.8cm] (kb)
    {Knowledge\\Bases\\[-1pt]{\scriptsize\itshape YAGO, GO,}\\[-2pt]{\scriptsize\itshape UMLS, \ldots}};
\node[box, right=1.0cm of kb, text width=1.8cm] (lp)
    {Logic\\Program $\Pi$};
\draw[arr] (kb) -- node[lbl] {formalize} (lp);

% --- Stage 2 ---
\node[box, right=1.0cm of lp, text width=1.8cm] (dt)
    {Defeasible\\Theory $\mathcal{D}$};
\draw[arr] (lp) -- node[lbl] {$\kappa$} (dt);

% --- Stage 3: three levels, vertically stacked ---
\node[levelbox, right=1.2cm of dt, yshift= 0.85cm] (l3) {L3 Defeater};
\node[levelbox, right=1.2cm of dt]                  (l2) {L2 Rule};
\node[levelbox, right=1.2cm of dt, yshift=-0.85cm]  (l1) {L1 Fact};

\draw[arr] (dt.east) -- ++(0.35,0) |- (l3.west);
\draw[arr] (dt.east) -- (l2.west);
\draw[arr] (dt.east) -- ++(0.35,0) |- (l1.west);

% --- Stage 4: rendering modalities as a single block ---
\node[modbox, right=1.2cm of l2, text width=1.6cm, minimum height=2.2cm]
    (render) {M1 Narrative\\[1pt]M2 Semi-formal\\[1pt]M3 Annotated\\[1pt]M4 Formal\\[1pt]M5 Visual};

\draw[arr] (l3.east) -- (render.west |- l3.east);
\draw[arr] (l2.east) -- (render.west);
\draw[arr] (l1.east) -- (render.west |- l1.east);

% --- Edge annotations ---
\node[font=\scriptsize\itshape, text=black!50, above=0.08cm of l3]
    {ablate + verify};
\node[font=\scriptsize\itshape, text=black!50, above right=-0.1cm and 0.15cm of render.north west]
    {render};

\end{tikzpicture}
