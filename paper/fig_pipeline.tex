% DeFAb combined pipeline + L1/L2/L3 generation figure — compact version
% Top row: pipeline arc. Bottom: three columns showing the same bear theory
% ablated three ways, one per level, with gold answers and key distractors.
\begin{tikzpicture}[
    >=stealth,
    node distance=0.2cm and 0.5cm,
    % pipeline boxes
    pipebox/.style={
        draw=defabBlue!70, rounded corners=2pt,
        font=\fontsize{6.5}{7.5}\selectfont, inner sep=3pt,
        text centered, align=center,
        fill=defabBlue!6, line width=0.4pt, minimum height=0.55cm
    },
    % column headers
    levhdr/.style={
        draw=defabTeal!70, rounded corners=2pt,
        font=\fontsize{6.5}{7.5}\selectfont\bfseries,
        inner sep=2.5pt, text centered, align=center,
        fill=defabTeal!12, line width=0.4pt,
        minimum width=3.8cm, minimum height=0.42cm, text=defabTeal
    },
    % theory lines (compacted mini-box style as inline text)
    linebox/.style={
        draw=defabGray!40, rounded corners=1pt,
        font=\fontsize{5.8}{6.5}\selectfont, inner sep=2pt, align=left,
        fill=white, line width=0.35pt, minimum width=3.8cm
    },
    ablline/.style={
        draw=defabRed!65, rounded corners=1pt,
        font=\fontsize{5.8}{6.5}\selectfont\bfseries,
        inner sep=2pt, align=left,
        fill=defabRed!7, line width=0.4pt, minimum width=3.8cm, text=defabRed
    },
    goldline/.style={
        draw=defabTeal!75, rounded corners=1pt,
        font=\fontsize{5.8}{6.5}\selectfont\bfseries,
        inner sep=2pt, align=left,
        fill=defabTeal!9, line width=0.4pt, minimum width=3.8cm, text=defabTeal!80
    },
    distline/.style={
        draw=defabGray!35, rounded corners=1pt,
        font=\fontsize{5.5}{6}\selectfont\itshape,
        inner sep=2pt, align=left,
        fill=defabGray!4, line width=0.3pt, minimum width=3.8cm, text=defabGray
    },
    modbox/.style={
        draw=defabGold!70, rounded corners=2pt,
        font=\fontsize{6.5}{7.5}\selectfont,
        inner sep=2.5pt, text centered, align=center,
        fill=defabGold!8, line width=0.4pt, minimum height=0.42cm
    },
    parr/.style={->, thick, color=defabBlue!55, line width=0.6pt},
    sarr/.style={->, thin,  color=defabGray!65, line width=0.45pt},
]

%% ---------------------------------------------------------------
%% TOP ROW: pipeline arc (compressed)
%% ---------------------------------------------------------------
\node[pipebox, text width=1.45cm] (kb) at (0,0)
    {KBs\\{\fontsize{5.5}{6}\selectfont\color{defabGray}15+ sources}};
\node[pipebox, text width=1.3cm, right=0.65cm of kb] (lp) {Logic\\Program $\Pi$};
\node[pipebox, text width=1.4cm, right=0.65cm of lp] (dt) {Defeasible\\Theory $\D$};
\node[pipebox, text width=1.3cm, right=0.65cm of dt] (ab) {Ablate\\critical $e$};
\node[pipebox, text width=1.45cm, right=0.65cm of ab] (vr)
    {\textbf{Verifier}\\$O(|\D|^3)$};

\draw[parr] (kb) -- node[above, font=\fontsize{5}{6}\selectfont\itshape,
    text=defabGray] {form.} (lp);
\draw[parr] (lp) -- node[above, font=\fontsize{5}{6}\selectfont\itshape,
    text=defabGray] {$\kappa$} (dt);
\draw[parr] (dt) -- node[above, font=\fontsize{5}{6}\selectfont\itshape,
    text=defabGray] {ablate} (ab);
\draw[parr] (ab) -- node[above, font=\fontsize{5}{6}\selectfont\itshape,
    text=defabGray] {verify} (vr);

%% ---------------------------------------------------------------
%% BOTTOM REGION: L1 / L2 / L3 columns
%% Shared bear-hibernation theory:
%%   facts: bear(G), bear(P), arctic(P), seal_hunter(P), winter_active(P)
%%   r_s:  bear(X) -> mammal(X)
%%   r_d:  bear(X) => hibernates(X)
%%   r*:   bear(X),arctic(X),sh(X) ~> not hib(X); r* > r_d
%% ---------------------------------------------------------------

%% ---- L1 column ----
\node[levhdr] (h1) at (-3.9, -0.9)  {L1: Fact Completion};

\node[linebox, below=0.14cm of h1, text width=3.8cm] (t1) {
  \textbf{Theory:}
  \texttt{bear(G)}\ \texttt{bear(P)}\ \texttt{arctic(P)\ seal\_h(P)}\\
  $r_d$: \texttt{bear(X) $\Rightarrow$ hibernates(X)}\\
  $r^*$: \texttt{bear(X),arctic(X) $\leadsto\neg$hib(X)}
};
\node[ablline, below=0.08cm of t1, text width=3.8cm] (a1) {
  Remove fact \texttt{bear(grizzly)} $\leftarrow$ ablated!\\
  \textbf{Target:} \texttt{hib(grizzly)} -- chain broken
};
\node[goldline, below=0.08cm of a1, text width=3.8cm] (g1) {
  \textbf{Gold:} \texttt{bear(grizzly)} restores chain
};
\node[distline, below=0.06cm of g1, text width=3.8cm] (d1) {
  Distractor: \texttt{mammal(grizzly)} [wrong predicate]
};

%% ---- L2 column ----
\node[levhdr] (h2) at (0.0, -0.9)  {L2: Rule Abduction};

\node[linebox, below=0.14cm of h2, text width=3.8cm] (t2) {
  \textbf{Theory:}
  \texttt{bear(G)}\ \texttt{bear(P)}\ \texttt{arctic(P)\ seal\_h(P)}\\
  $r_s$: \texttt{bear(X) $\to$ mammal(X)}\\
  $r^*$: \texttt{bear(X),arctic(X) $\leadsto\neg$hib(X)}
};
\node[ablline, below=0.08cm of t2, text width=3.8cm] (a2) {
  Remove rule $r_d$: \texttt{bear(X) $\Rightarrow$ hib(X)}\\
  \textbf{Target:} \texttt{hib(grizzly)} -- no rule!
};
\node[goldline, below=0.08cm of a2, text width=3.8cm] (g2) {
  \textbf{Gold:} \texttt{bear(X) $\Rightarrow$ hib(X)} reconstructed
};
\node[distline, below=0.06cm of g2, text width=3.8cm] (d2) {
  Distractor: \texttt{mammal(X) $\Rightarrow$ hib(X)} [too broad]
};

%% ---- L3 column ----
\node[levhdr] (h3) at (3.9, -0.9)  {L3: Defeater Abduction};

\node[linebox, below=0.14cm of h3, text width=3.8cm] (t3) {
  \textbf{Theory:}
  \texttt{bear(P)\ arctic(P)\ seal\_h(P)\ winter\_act(P)}\\
  $r_d$: \texttt{bear(X) $\Rightarrow$ hib(X)}
};
\node[ablline, below=0.08cm of t3, text width=3.8cm] (a3) {
  Remove $r^*$ --- theory predicts \texttt{hib(polar)},\\
  but \texttt{winter\_active(polar)}: \textbf{anomaly!}
};
\node[goldline, below=0.08cm of a3, text width=3.8cm] (g3) {
  \textbf{Score 1.0:} $r^*$ restored, $r^*\!\succ\!r_d$ (conservative)
};
\node[distline, below=0.06cm of g3, text width=3.8cm] (d3) {
  Score 0.5: \texttt{$\neg$hib(X) :- bear(X)} [not conservative]
};

%% ---- Arrows from pipeline ablation box down to column headers ----
\draw[sarr] (ab.south) .. controls +(-0.2,-0.25) and +(0.1,0.25) .. (h1.north);
\draw[sarr] (ab.south) -- (h2.north);
\draw[sarr] (ab.south) .. controls +(0.2,-0.25) and +(-0.1,0.25) .. (h3.north);

%% ---- Modality strip ----
\node[modbox, below=0.18cm of d1, text width=3.6cm, minimum height=0.42cm] (m1)
    {M1 Narrative};
\node[modbox, below=0.18cm of d2, text width=3.6cm, minimum height=0.42cm] (m2)
    {M2--M4 Formal};
\node[modbox, below=0.18cm of d3, text width=3.6cm, minimum height=0.42cm] (m3)
    {M5 Visual};

\draw[sarr] (d1.south) -- (m1.north);
\draw[sarr] (d2.south) -- (m2.north);
\draw[sarr] (d3.south) -- (m3.north);

\draw[{<[scale=0.6]}-{>[scale=0.6]}, color=defabGold!65, line width=0.4pt]
    (m1.east) -- (m2.west);
\draw[{<[scale=0.6]}-{>[scale=0.6]}, color=defabGold!65, line width=0.4pt]
    (m2.east) -- (m3.west);
\node[font=\fontsize{5.5}{6}\selectfont\itshape, text=defabGold!80,
      below=0.04cm of m2, anchor=north] {Five rendering modalities (each instance)};

\end{tikzpicture}
