# BLANC: Defeasible Abduction Benchmark
# Requirements file for reproducibility
# Author: Anonymous Authors
# Date: 2026-02-13

# Core dependencies
numpy>=2.0.0
scipy>=1.15.0

# Codec dependencies
python-Levenshtein>=0.27.0  # D2 decoder - fuzzy string matching
lark>=1.3.0                 # D3 decoder - semantic parsing

# Testing
pytest>=8.4.0
pytest-cov>=6.2.0
pytest-benchmark>=5.2.0
pytest-timeout>=2.4.0
pytest-xdist>=3.8.0
pytest-mock>=3.15.0

# Week 8: LLM Evaluation Infrastructure
openai>=1.50.0              # OpenAI GPT-4o API
anthropic>=0.38.0           # Anthropic Claude 3.5 API
google-generativeai>=0.8.0  # Google Gemini 1.5 Pro API
ollama>=0.3.0               # Local Llama models via Ollama

# Utilities for evaluation
tenacity>=8.0.0             # Retry logic with exponential backoff
tqdm>=4.66.0                # Progress bars
pydantic>=2.0.0             # Data validation
python-dotenv>=1.0.0        # Environment variable management

# Optional: Ontology extraction (for KB curation - not required for benchmarking)
# rdflib>=7.0.0  # Uncomment if you need OpenCyc extraction

# Optional: Live SC2 environment (requires StarCraft II binary)
# burnysc2>=7.0  # pip install blanc[sc2live]
